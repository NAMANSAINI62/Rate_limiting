# Project Rules

- **Git Commits**: Whenever asked to push code to GitHub, ALWAYS ask the user what to write in the commit message before running the `git commit` and `git push` commands. Do not generate a commit message automatically. Wait for the user to provide their explanation.
